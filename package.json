{
  "name": "snake-multiplayer",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network ritual",
    "build:wallet": "npx esbuild wallet/privy-bridge.jsx --bundle --format=iife --outfile=public/wallet-bundle.js --jsx=automatic --platform=browser --minify --define:process.env.NODE_ENV='\"production\"'"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^4.9.6",
    "@privy-io/react-auth": "^3.23.1",
    "dotenv": "^16.4.5",
    "esbuild": "^0.28.0",
    "hardhat": "^2.22.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  }
}
